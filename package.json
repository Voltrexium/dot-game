{
  "name": "dot-game",
  "private": true,
  "scripts": {
    "config": "node scripts/generate-config.mjs",
    "build": "npm run config",
    "prestart": "npm run config",
    "start": "serve .",
    "dev": "npm run config && serve ."
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
